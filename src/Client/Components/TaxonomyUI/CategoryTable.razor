@using MudBlazor
@* Tabelle der Kategorien – global + persönlich. Global-Einträge können nicht gelöscht werden. *@

<MudPaper Elevation="2" Style="padding: 24px;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px;">
        <MudText Typo="Typo.h6">Kategorien</MudText>
        <MudButton OnClick="@OnRefresh"
                   Variant="Variant.Text"
                   Color="Color.Primary"
                   StartIcon="@Icons.Material.Filled.Refresh"
                   Disabled="@Loading">
            Aktualisieren
        </MudButton>
    </div>

    @if (Loading)
    {
        <div style="text-align: center; padding: 24px;">
            <MudProgressCircular Indeterminate="true" />
        </div>
    }
    else if (!Items.Any())
    {
        <div style="text-align: center; padding: 24px;">
            <MudIcon Icon="@Icons.Material.Filled.Category" Size="Size.Large" Color="Color.Secondary" />
            <MudText Typo="Typo.body2" Color="Color.Secondary" Class="mt-2">Keine Kategorien vorhanden.</MudText>
        </div>
    }
    else
    {
        <MudDataGrid Items="@Items" Filterable="true" SortMode="SortMode.Single" Dense="true">
            <Columns>
                <TemplateColumn Title="Farbe" Sortable="false" HeaderStyle="width:60px">
                    <CellTemplate>
                        <div style="width:24px;height:24px;border-radius:50%;background-color:@context.Item.Color;border:1px solid #ccc;"></div>
                    </CellTemplate>
                </TemplateColumn>

                <PropertyColumn Property="x => x.Label" Title="Name" />

                <PropertyColumn Property="x => x.IsGlobal" Title="Sichtbarkeit">
                    <CellTemplate>
                        <MudChip T="string"
                                 Size="Size.Small"
                                 Color="@(context.Item.IsGlobal ? Color.Info : Color.Default)">
                            @context.Item.ScopeLabel
                        </MudChip>
                    </CellTemplate>
                </PropertyColumn>

                <PropertyColumn Property="x => x.CreatedAt" Title="Erstellt" Format="dd.MM.yyyy" />

                <TemplateColumn Title="" Sortable="false" HeaderStyle="width:80px">
                    <CellTemplate>
                        @if (!context.Item.IsGlobal)
                        {
                            <MudButton Size="Size.Small"
                                       Color="Color.Error"
                                       Variant="Variant.Text"
                                       OnClick="@(() => OnDelete.InvokeAsync(context.Item.Id))">
                                Löschen
                            </MudButton>
                        }
                    </CellTemplate>
                </TemplateColumn>
            </Columns>
        </MudDataGrid>
    }
</MudPaper>

@code {
    [Parameter, EditorRequired] public IReadOnlyList<CategoryViewModel> Items   { get; set; } = [];
    [Parameter]                 public bool                             Loading { get; set; }
    [Parameter, EditorRequired] public EventCallback<Guid>             OnDelete { get; set; }
    [Parameter, EditorRequired] public EventCallback                   OnRefresh { get; set; }
}
